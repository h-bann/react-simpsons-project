import React from "react";

const text = (props) => {
  return (
    <div className={props.className}>
      <h3>We find stupid Simpsons quotes so you don't have to!</h3>
    </div>
  );
};

export default text;
